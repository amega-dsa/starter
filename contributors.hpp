#include <iostream>
#include <string>
using namespace std;
// make your functions here just like the function given below
void paraspatle(string arr[], int size)
{
    string name = "paraspatle";
    arr[size - 1] = name;
}
void satyamshukla(string arr[], int size)
{
    string name = "satyamshukla";
    arr[size - 1] = name;
}
void prathameshpatil(string arr[], int size)
{
    string name = "prathameshpatil";
    arr[size - 1] = name;
}
void shubhagarwal(string arr[], int size)
{
    string name = "shubhagarwal";
    arr[size-1] = name;
}
void shubhasri(string arr[], int size)
{
    string name = "shubhasri";
    arr[size - 1] = name;
}
void bhaveshchaudhari(string arr[], int size)
{
    string name = "bhaveshchaudhari";
    arr[size - 1] = name;
}
void tanmayeegosavi(string arr[],int size)
{
    string name="tanmayeegosavi";
    arr[size-1]=name;
}